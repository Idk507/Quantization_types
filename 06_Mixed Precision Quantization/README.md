Mixed Precision Quantization. Instead of a single bit width for the whole network, this assigns different bit widths to different layers based on a sensitivity measure, commonly the top eigenvalue or trace of the loss Hessian, as in HAWQ, so sensitive layers stay at higher precision and flat, forgiving layers go as low as two or three bits. This is a natural next step once you understand that not all layers tolerate quantization equally.
