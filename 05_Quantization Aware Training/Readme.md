Quantization Aware Training, QAT. This introduces simulated quantization, called fake quantization, into the forward pass during training or fine tuning, so the model learns weights that are robust to rounding error, using the straight through estimator to let gradients pass through the non differentiable rounding step. Learn this after PTQ because it is more expensive and is only necessary when PTQ alone does not meet your accuracy bar.
