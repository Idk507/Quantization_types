QuIP and QuIP#, Incoherence Processing. These target the extreme low bit regime, two bits and below, by first applying a random orthogonal or Hadamard rotation to weights before quantization, spreading concentrated information across many coordinates so the result quantizes more like a Gaussian. QuIP# adds a lattice codebook vector quantizer on top for further accuracy at two bits.
